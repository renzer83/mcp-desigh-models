{
  "id": "flow_design_factory",
  "nome": "Design Factory — idea → telas (qwen/z-image) + assets + spec completa",
  "ativo": true,
  "nos": [
    {
      "id": "compositor",
      "tipo": "agente",
      "nome": "compositor",
      "pos": {
        "x": -320,
        "y": -120
      },
      "params": {
        "papel": "Senior UI/UX + brand design director. You compose real product screens from real component patterns. You do NOT render anything yourself — a downstream node renders your plan as placeholder-text skeletons. You never invent AI-looking filler; you compose from components that actually exist.",
        "objetivo": "Product brief (injected per run):\n\n{{ param.brief }}\n\n1. Decide the authenticated product's core screens (max 6 unless the brief asks for more) with ONE shared sidebar (single-word nav items, once each) identical across screens.\n2. For each screen use WebSearch to ground the composition in real component patterns (shadcn/ui, Tailwind UI blocks): sidebar, stat cards, data tables, config forms, charts, side detail panels. Note the components per screen inside the layout prose.\n3. Decide the content assets: brand (logo mark, background) and per-screen imagery (hero, illustration, empty-state). Keep them minimal and real.\n4. Choose palette + accent in words (dark theme by default), consistent across screens.\n5. Return the plan as JSON. Each screen's `layout` is PROSE of the main panel composed from those components — describe STRUCTURE not copy (the render is placeholder-text). Do NOT render and do NOT write the final spec.\n\nReturn JSON: {product, theme, palette, accent, sidebar:[...], screens:[{screen,active,layout,tags:[...]}], assets:[{role,type,prompt}]}.",
        "provider": "claude",
        "modelo": "opus-4.8",
        "pasta": "/data/studio",
        "mcp": "(nenhum)",
        "ferramentas": "WebSearch",
        "esquema": "{\"type\":\"object\",\"required\":[\"product\",\"screens\"],\"properties\":{\"product\":{\"type\":\"string\"},\"theme\":{\"type\":\"string\",\"enum\":[\"dark\",\"light\"]},\"palette\":{\"type\":\"string\"},\"accent\":{\"type\":\"string\"},\"sidebar\":{\"type\":\"array\",\"items\":{\"type\":\"string\"}},\"screens\":{\"type\":\"array\",\"items\":{\"type\":\"object\",\"required\":[\"screen\",\"layout\"],\"properties\":{\"screen\":{\"type\":\"string\"},\"active\":{\"type\":\"string\"},\"layout\":{\"type\":\"string\"},\"tags\":{\"type\":\"array\",\"items\":{\"type\":\"string\"}}}}},\"assets\":{\"type\":\"array\",\"items\":{\"type\":\"object\",\"required\":[\"role\",\"type\",\"prompt\"],\"properties\":{\"role\":{\"type\":\"string\"},\"type\":{\"type\":\"string\",\"enum\":[\"photo\",\"illustration\",\"icon\",\"background\",\"logo\"]},\"prompt\":{\"type\":\"string\"}}}}}}",
        "timeout_s": 1800
      }
    },
    {
      "id": "telas",
      "tipo": "telas",
      "nome": "telas",
      "pos": {
        "x": 120,
        "y": -120
      },
      "params": {
        "produto": "{{ entrada.compositor.product }}",
        "tema": "{{ entrada.compositor.theme || 'dark' }}",
        "modelo": "z-image",
        "formato": "web-mockup",
        "paleta": "{{ entrada.compositor.palette }}",
        "acento": "{{ entrada.compositor.accent }}",
        "sidebar": "{{ entrada.compositor.sidebar }}",
        "telas": "{{ entrada.compositor.screens }}",
        "assets": "{{ entrada.compositor.assets }}",
        "prefixo": "studio/design"
      }
    },
    {
      "id": "analyst",
      "tipo": "agente",
      "nome": "analyst",
      "pos": {
        "x": 560,
        "y": -120
      },
      "params": {
        "papel": "Extremely thorough principal design engineer. You look at what was generated and produce the complete, buildable specification — the maximum useful information — so the caller can build the product without guessing.",
        "objetivo": "Manifest rendered by the telas node: {{ entrada.telas.manifest_path }}\nProduct: {{ entrada.compositor.product }} | sidebar: {{ entrada.compositor.sidebar }} | palette: {{ entrada.compositor.palette }} | accent: {{ entrada.compositor.accent }}\n\n1. Read the manifest.json at that path AND Read every screen image it lists (use the Read tool; you can see images). Images are placeholder-text skeletons: read them for LAYOUT, hierarchy, proportion, colour and component placement. Do NOT trust text pixels — you write the real copy.\n2. Produce the full spec (see spec-schema.json), with maximum detail: design_tokens (real hexes per role, typography families + scale + weights, spacing, radii, borders, shadows, motion easing+durations); global (sidebar labels+icons, topbar, grid, breakpoints); screens[] (route, purpose, active_nav, image_ref, a component_tree with type+props+REAL copy+data_shape, regions, states loading/empty/error, responsive per breakpoint); components[] (real components + source + props); assets (brand + per_screen mapped to files + placement); copy (every real string keyed by id).\n3. Be exhaustive and internally consistent; concrete values over vague notes. This spec is the single source of truth for the build.\n\nReturn ONLY the spec JSON.",
        "provider": "claude",
        "modelo": "opus-5",
        "pasta": "/data/studio",
        "mcp": "(nenhum)",
        "ferramentas": "Read",
        "esquema": "",
        "timeout_s": 1800
      }
    }
  ],
  "ligacoes": [
    {
      "de": "compositor",
      "para": "telas"
    },
    {
      "de": "telas",
      "para": "analyst"
    }
  ]
}
