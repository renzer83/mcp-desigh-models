{
  "name": "mcp-design-models",
  "version": "0.3.0",
  "description": "Thin MCP server that turns a product idea into UI screen mockups and a complete buildable spec by running the Studio 'Design Factory' flow (which owns the render queue and GPU guard).",
  "type": "module",
  "bin": {
    "mcp-design-models": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "node --experimental-strip-types src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "undici": "^6.21.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
